# Pipeline breakdown

- generated: **2026-08-07T02:02:33.360680+00:00**
- commit: `6a6adb7`
- flags raised: **4** (concentration, gap, risk)
- artifacts missing: **0**

Generated by `python -m stages.breakdown`. **Every number is copied from the artifact that owns it**, named beside it — this page measures nothing, so it cannot disagree with a stage. Re-run the stage, not this, to change a number.

---

## §A — Flags raised this run

Mechanical rules only: a threshold crossed, a document that does not quote the current pair. None of these is a verdict — each names what to look at.

### `risk` — the 95% target is missed on the lockbox

rev8 scores **0.9269** at coverage 75.6%. Every development number in §2 is an upper bound, not an estimate.

### `risk` — per-subject macro-F1 spread is wide

**0.8553 (rev5) – 0.9687 (rev3)** over 7 subjects. A single unusual subject moves the shipped threshold more than any tuning does.

### `concentration` — the worst corpus files are a cluster

**12 of 78 files** commit to under 50% of their rows, across 1 variant(s) and 4 session(s); dominant reason `model_split` on 12 of them. They **concentrate on one variant (`fb5ea2c2`) in 4 session(s): 20251230, 20260107, 20260108, 20260109** — that is a session-level cause, not 12 hard recordings. Nobody has looked at one by eye: `python -m stages.s3_physics.inspect_window`.

### `gap` — some hardware cannot be served at all

**13 file(s)** refuse with `UnknownVariantError`. Each needs one paired raw+annotated recording to map its sagittal axis; signal-only detection scores below chance.

---

## §0 — What the corpus is

Three populations, and confusing them is the most common way to misread every number below. Only the middle one has ground truth.

| population | count | what it is |
|---|---|---|
| raw device logs (`data/raw`) | 91 | what S1 cleans and `label_all` sweeps — **no ground truth** |
| label-pure windows | 5,984 | what S2 and S3 are *measured* on — **has ground truth** |
| rows labelled by the sweep | 2,594,823 | what a caller receives — **no ground truth** |

Development subjects (everything below is measured on these): **rev13, rev2, rev3, rev4, rev5, rev6, rev7**.

Sealed lockbox subject: **rev8** — single use. See §2 for what it scored and `caveats.md` §3.2 before quoting it.

### Excluded before any of the above

**1 trial(s)** are dropped as the corpus is loaded (`data/corpus.json`), so every count on this page is already net of them and no stage below can put them back. The manifest is edited **by hand**; the `disagree` column is `label_audit`'s, which reads the RAW corpus and so still sees these trials. That is what makes the exclusion checkable instead of self-confirming — a row whose evidence has gone thin still shows its number here.

| rev | trial | windows | disagree | why it went |
|---|---|---|---|---|
| `rev13` | 4 | 62 | **0.9516** | two runs concatenated into one file - a demonstrated label error whose evidence is internal to the file; see runs/regen/s3_physics/label_audit |

### Hardware variants, by whether they can be served

| variant | files | servable | sagittal axis |
|---|---|---|---|
| `fb5ea2c2` | 62 | **yes** | `Deg_Y` |
| `0fda484e` | 11 | **yes** | `Deg_Y` |
| `4bfd6ab2` | 5 | **yes** | `Deg_Y` |
| `e5f2660f` | 11 | **no — unmapped** | — |
| `86069795` | 2 | **no — unmapped** | — |

An unmapped variant needs **one paired raw+annotated recording** to fix. Signal-only axis detection scores below chance (`DOMAIN_NOTES` §6.2), so guessing is not available.

---

## §1 — S1 clean

*Source: `runs/regen/s1_clean/`*

### Partition gate

**91 segmented + 0 quarantined = 91 files accounted for, 0 unaccounted.** Asserted by the stage, not hoped.

|  | count |
|---|---|
| files segmented | 91 |
| files quarantined (whole-file reject) | 0 |
| segments found | 141 |
| segments usable | 94 |
| segments dropped | 47 |
| usable duration | 481.5 min |

### Quarantined files — the complete ledger

*(nothing to report)*

### Dropped segments, by cause

| cause | segments | files | what it is |
|---|---|---|---|
| export preamble (`seg 0`, ≤12 rows) | **35** | 35 | one systematic artifact — a short preamble before the first real gap. Not 35 independent data problems |
| genuinely short segment | 9 | 6 | a fragmented recording — worth a look if one file dominates |
| **rate matches no known acquisition rate** | **3** | 3 | **not a length problem** — the measured rate matches no era this pipeline knows |

| file | seg | measured rate | reason |
|---|---|---|---|
| `00095_70_2026_5_15_11_38_0.csv` | 1 | 0.000 Hz | rate nan Hz matches no known acquisition rate |
| `00156_92_2026_6_26_9_52_0.csv` | 0 | 248.062 Hz | rate 248.062 Hz matches no known acquisition rate |
| `00157_92_2026_6_26_9_52_0.csv` | 0 | 248.062 Hz | rate 248.062 Hz matches no known acquisition rate |

Most fragmented: `00038_69_2026_1_8_15_11_0.csv` lost **4** segments.

### Resample method, by usable segment

| method | segments |
|---|---|
| `grid_aligned` | 73 |
| `decimate_5x_fir` | 15 |
| `interp_to_grid` | 6 |

`decimate_5x_fir` is the 500 Hz era. Naive `[::5]` is never used — it folds everything above 50 Hz into the gait band as a full-amplitude fake signal (`DOMAIN_NOTES` §2.5).

### Trust flags — raised, nothing dropped for them

| check | files |
|---|---|
| channel trust resolved from signal | 72 |
| channel trust abstained (too static → documented convention) | 19 |
| **channel trust anomaly** (detected disagrees with documented) | **0** |
| `Deg` channels flagged drift-contaminated | 9 across 8 files |

Drift-contaminated `Deg` channels are flagged, not dropped — the raw superset is kept. A channel whose value tracks session time is measuring integration drift, not orientation:

| file | channels |
|---|---|
| `00324_63_2025_10_24_15_56_0.csv` | `L_Deg_Z` |
| `00041_69_2026_1_8_17_19_0.csv` | `R_Deg_Z` |
| `00046_69_2025_11_6_14_52_0.csv` | `L_Deg_Z` |
| `00008_69_2026_1_14_10_48_0.csv` | `L_Deg_Z` |
| `00038_69_2026_1_15_11_28_0.csv` | `R_Deg_Z` |
| `00078_69_2026_1_28_14_30_0.csv` | `L_Deg_Z` |
| `00084_69_2026_1_28_15_45_0.csv` | `L_Deg_Z` |
| `00101_69_2026_1_28_13_38_0.csv` | `L_Deg_Z`, `R_Deg_Z` |

**What S1 does not resolve:** which axis is *sagittal*. That has no in-file signature and is a variant lookup in `stages/s2_ml/transform.py`. It is the most consequential thing handed downstream unresolved, and it was wrong for the majority variant until 2026-08-03 (`caveats.md` §5).

---

## §2 — S2 ml

*Source: `runs/regen/s2_ml/`, `runs/keep/s2_ml/`, `stages/s2_ml/champion_spec.json`*

### The champion

|  |  |
|---|---|
| spec | `extratrees400_drop_moments38` |
| model | `ExtraTreesClassifier` |
| params | `n_estimators=400`, `random_state=0`, `n_jobs=-1`, `class_weight=balanced` |
| features | 38 |
| window / training stride | 2.0 s / 2.0 s (non-overlapping) |
| inference stride | 0.25 s |
| CV | `LeaveOneGroupOut(rev)` |
| shipped preset | `balanced` = threshold 0.85 |

### Why this champion, and what was rejected

ExtraTrees over 38 features, non-overlapping 2 s windows at the canonical 100 Hz. Chosen against RandomForest on identical features, folds and params over 5 seeds: equal precision at threshold 0.85 while committing to 87.6% of windows against 81.9% (see the train.py docstring). The four dropped angular-velocity higher moments are removed for parsimony, NOT for a gain: re-measured 2026-08-04 as the MEAN over 5 seeds within each feature set, 42 vs 38 features is LORO macro-F1 0.9194 -> 0.9186 and coverage at 0.85 0.8760 -> 0.8791, against a within-set spread of 0.0019 and 0.0017 respectively. Kurtosis and peak-ratio are noise-sensitive by construction and sat at the bottom of the importance table (0.0047-0.0055, ranks 39-42 of 42); four features that buy nothing measurable are four fewer things to explain and to keep trustworthy across a new subject. NOTE on reading any number here: single-seed macro-F1 differences below ~0.004 are NOT interpretable at this corpus size. An earlier version of this file compared seed-0 runs against a +/-0.0003 band, which is the standard error of a 5-seed mean, not the spread of one draw; that error is what the 2026-08-04 sweep corrected. Compare means over seeds, and quote the within-set spread beside them.

**Rejected, with the measurement:**

- drop the zeroing family (L/R_angvel_LPF_mean, L/R_ang_med_rest): NOT rejected on the evidence, and retained only because nothing yet argues for removing them. The original entry cited macro-F1 0.9193 -> 0.9168 and concluded these carry real signal. Re-measured over 5 seeds per feature set: 0.9186 vs 0.9185, a difference of 0.0001, against within-set spreads of 0.0017 and 0.0042. The 0.9168 was seed 0, the near-bottom of that set's own 0.9166-0.9208 range; seed 2 puts the same drop ABOVE the champion. The per-subject cuff-zeroing argument from the sibling repo is therefore neither confirmed nor refuted here - this corpus cannot resolve a difference that small.
- drop the bottom 8 by importance: macro-F1 0.9193 -> 0.9176, single seed, never swept. Below the ~0.004 resolution above, so read it as 'no measured difference', not as a regression. The earlier gloss ('the regression is the zeroing half, not the moments') attributed a delta this corpus cannot separate.
- cut to 27 features by importance density, dropping the moments, the zeroing family, L/R_angvel_skew, L/R_angvel_dom_hz, L/R_cycle_s and angvel_LR_lag_s: macro-F1 0.9186 -> 0.9171 over 5 seeds (spread 0.0017 vs 0.0034), coverage 0.8791 -> 0.8811. The closest thing to a real cost measured so far, and still marginal. 11 fewer features is a live option, not a rejected one; it is not taken because nothing needs it.

Dropped for parsimony: `L_angvel_kurt`, `R_angvel_kurt`, `L_angvel_peakratio`, `R_angvel_peakratio`.

### The champion/challenger ledger

*Source: `runs/keep/s2_ml/experiments.jsonl`, `proposals.jsonl` — written by the `s2_experiment` step. An agent proposes and a second criticises; neither can promote. `experiment.decide()` gates on measured macro-F1 and logs the reason either way.*

Incumbent: **`extratrees400_drop_moments38`** at macro-F1 0.9196, recorded 2026-08-04T03:16:59Z (`b00fba0`).

| experiment | macro-F1 | features | outcome |
|---|---|---|---|
| `extratrees400_drop_moments38` | 0.9196 | 38 | **promoted** — seed: the tracked champion spec, re-measured as the incumbent |
| `extratrees400_drop_moments38` | 0.9196 | 38 | **promoted** — seed: the tracked champion spec, re-measured as the incumbent |
| `min_leaf2_extratrees` | 0.9211 | 42 | rejected — macro-F1 0.9211 vs champion 0.9196 (+0.0016); below the 0.005 promotion margin |

A further **16** entries were measured on an earlier basis — a different model over a different feature set — and are not comparable to anything above. They are kept because an idea that lost is still an idea that lost, and dropped from this table because a macro-F1 column that mixes two bases invites exactly the subtraction `decide()` refuses to make:

| experiment | macro-F1 (earlier basis) | outcome |
|---|---|---|
| `baseline_v1` | 0.8730 | promoted then |
| `drop_absolute_angle` | 0.8758 | rejected |
| `wider_window_4s` | 0.8150 | rejected |
| `window_4s_stride_2s` | 0.8188 | rejected |
| `drop_bad_band_frac` | 0.8711 | rejected |
| `regularize_min_samples_leaf10` | 0.8181 | rejected |
| `drop_offset_only` | 0.8862 | promoted then |
| `drop_angvel_dom_hz` | 0.8886 | promoted then |
| `drop_ang_mean_only` | 0.8926 | rejected |
| `drop_ang_absmean_only` | 0.8912 | rejected |
| `drop_static_offset_family` | 0.8977 | promoted then |
| `drop_offset_family_and_domhz` | 0.8946 | rejected |
| `max_features_full_split` | 0.8764 | rejected |
| `drop_angvel_mean_only` | 0.8894 | rejected |
| `class_weight_balanced_subsample` | 0.8997 | rejected |
| `cap_max_depth_8` | 0.8938 | rejected |

Proposals that never cost a fit — the critic reviews before training, which is the cycle's whole cost asymmetry:

| proposal | why it did not run |
|---|---|
| `balanced_class_weight` | critic said reject |
| `drop_offset_family_and_band_frac` | critic said reject |
| `more_trees_1000` | critic said reject |
| `drop_angvel_ptp` | critic said reject |
| `drop_offset_family_and_angvel_mean` | critic said reject |
| `drop_angvel_lr_corr` | critic said reject |

#### Estimator comparison — identical features, folds and params

| model | seeds | macro-F1 | accuracy | balanced acc | coverage @0.85 | selective acc @0.85 |
|---|---|---|---|---|---|---|
| ExtraTrees | 5 | 0.9194 | 0.9625 | 0.8995 | 0.8760 | 0.9887 |
| RandomForest | 5 | 0.9138 | 0.9568 | 0.9246 | 0.8189 | 0.9889 |

The deciding line is coverage at equal precision: same quality of answer, more answers.

#### Feature-set resolution — 5 seeds *within* each feature set

| feature set | features | seeds | mean macro-F1 | min | max | spread | mean coverage @0.85 |
|---|---|---|---|---|---|---|---|
| `all42` | 42 | 5 | 0.9194 | 0.9183 | 0.9202 | 0.0019 | 0.8760 |
| `moments38` | 38 | 5 | 0.9186 | 0.9178 | 0.9196 | 0.0017 | 0.8791 |
| `zeroing38` | 38 | 5 | 0.9185 | 0.9166 | 0.9208 | 0.0042 | 0.8784 |
| `top27` | 27 | 5 | 0.9171 | 0.9154 | 0.9188 | 0.0034 | 0.8811 |

**Single-seed macro-F1 differences below ~0.0042 are not interpretable at this corpus size.** The spread is not constant across feature sets, so there is no one noise band to quote; compare means over seeds and carry the within-set spread alongside.

#### Feature ablations — single seed each

| variant | features | macro-F1 | vs shipped |
|---|---|---|---|
| **shipped** | 38 | 0.9196 | — |
| `bottom8` | 34 | 0.9176 | **below resolution** |
| `moments` | 38 | 0.9196 | **below resolution** |
| `top10` | 10 | 0.9094 | **worse** |
| `top14` | 14 | 0.9140 | **worse** |
| `top19` | 19 | 0.9140 | **worse** |
| `top27` | 27 | 0.9180 | **below resolution** |
| `zeroing` | 38 | 0.9168 | **below resolution** |

Every row is one seed, so the verdict column is read against the spread above, not against zero. Low importance is not droppability *and* low importance is not evidence of droppability — at this resolution most of these rows say nothing either way, which is the honest reading.

### Leave-one-rev-out — the headline

**macro-F1 0.9196** · accuracy 0.9624 · balanced accuracy 0.9013, over **5,984 label-pure windows**.

macro-F1 is the headline on purpose: this corpus is ~86% walking, so pooled accuracy is close to a walking detector's score.

| class | support | precision | recall | F1 |
|---|---|---|---|---|
| stand | 853 | 0.9110 | 0.8159 | **0.8609** |
| walk | 5,131 | 0.9699 | 0.9867 | 0.9783 |

Confusion (rows = truth):

|  | pred stand | pred walk |
|---|---|---|
| **stand** | 696 | 157 |
| **walk** | 68 | 5,063 |

Per-subject macro-F1 — the spread is what a new subject is drawn from:

| rev | macro-F1 |
|---|---|
| rev3 | 0.9687 |
| rev13 | 0.9664 |
| rev4 | 0.9540 |
| rev6 | 0.9147 |
| rev7 | 0.9111 |
| rev2 | 0.8927 |
| rev5 | 0.8553 |

Spread: **0.8553 – 0.9687** (rev5 worst, rev3 best) over 7 subjects.

### Selective accuracy — the coverage/accuracy pair

Coverage and accuracy are quoted together everywhere. Either alone is meaningless: abstain on all but the easiest window and accuracy reads 1.000.

#### Window level (`locoeval.json`)

| threshold | coverage | selective acc | worst subject | wrong windows kept |
|---|---|---|---|---|
| 0.50 | 100.00% | 0.9624 | 0.9241 (`rev5`) | 225 |
| 0.60 | 97.96% | 0.9708 | 0.9371 (`rev5`) | 171 |
| 0.70 | 94.90% | 0.9789 | 0.9433 (`rev5`) | 120 |
| 0.75 | 92.83% | 0.9825 | 0.9568 (`rev5`) | 97 |
| 0.80 | 90.84% | 0.9864 | 0.9635 (`rev5`) | 74 |
| 0.85 ← shipped | 88.00% | 0.9884 | 0.9699 (`rev5`) | 61 |
| 0.90 | 83.07% | 0.9907 | 0.9695 (`rev5`) | 46 |
| 0.95 | 72.94% | 0.9938 | 0.9813 (`rev5`) | 27 |
| 0.98 | 59.74% | 0.9980 | 0.9890 (`rev5`) | 7 |

#### Row level (`roweval_loro.json`) — the deliverable's own unit

Over 1,244,292 scored rows. Coverage runs lower than the window table because inference stride is 0.25 s and rows near a boundary inherit mixed windows.

| threshold | coverage | selective acc | worst subject | wrong rows kept |
|---|---|---|---|---|
| 0.50 | 100.00% | 0.9555 | 0.8799 (`rev5`) | 55,357 |
| 0.60 | 96.62% | 0.9682 | 0.8933 (`rev5`) | 38,215 |
| 0.70 | 92.75% | 0.9784 | 0.9099 (`rev5`) | 24,916 |
| 0.75 | 90.57% | 0.9824 | 0.9231 (`rev5`) | 19,865 |
| 0.80 | 87.94% | 0.9866 | 0.9383 (`rev5`) | 14,676 |
| 0.85 ← shipped | 84.66% | 0.9901 | 0.9525 (`rev5`) | 10,391 |
| 0.90 | 79.53% | 0.9942 | 0.9663 (`rev5`) | 5,727 |
| 0.95 | 67.83% | 0.9980 | 0.9884 (`rev5`) | 1,650 |
| 0.98 | 54.85% | 0.9994 | 0.9937 (`rev5`) | 390 |

**Why 0.85:** pooled accuracy clears the 95% target from 0.50 onward, so pooling is the wrong number to set a threshold by. 0.85 is the lowest threshold at which every held-out development subject independently clears it.

#### Seed band on that row — 5 seeds through the same path

Everything above is `random_state=0`, one draw. ExtraTrees picks split thresholds at random, so a different seed is a different 400 trees on identical data. These are 5 of them at threshold 0.85, scored row by row:

|  | seed 0 (shipped) | mean of 5 | min | max | spread |
|---|---|---|---|---|---|
| selective acc | 0.9901 | 0.9904 | 0.9901 | 0.9905 | 0.0004 |
| coverage | 84.66% | 84.59% | 84.52% | 84.66% | 0.14% |
| worst subject | 0.9525 | 0.9520 | 0.9509 | 0.9532 | 0.0023 |
| wrong rows kept | 10,391 | 10,107 | 9,974 | 10,391 | 417 |

**A single-seed change to selective accuracy below ~0.0004 is not a result.** Coverage moves ~0.14% and the worst subject moves ~0.0023 on the seed alone — so read the pair, never accuracy by itself: within this sweep the two are already anticorrelated, and buying accuracy by abstaining more is what the threshold column does for free.

The shipped draw is not a typical one — seed 0 has the **lowest** selective acc, the **highest** coverage and the **highest** wrong rows kept of the 5. It is the default, not a pick, but the quoted pair sits at the corner of the cloud rather than its middle.

This is the *seed* band and nothing else. It does not cover the corpus, the annotation, or the subject — between-subject spread is an order of magnitude larger (see the worst-subject column, and the lockbox below), and no number of seeds shrinks it.

**That column is the worst *development* subject.** `rev8` — sealed, single-use, and never seen by feature, model or threshold selection — is not in it and is worse. It is kept out on purpose: one cell holding both populations is exactly the conflation §0 warns about, and re-running it to keep the column tidy is the trade §7 forbids. Read the two side by side; the right-hand column is the floor a genuinely new subject is drawn from.

| threshold | worst development subject | `rev8` (lockbox, spent) | lockbox coverage | which is lower |
|---|---|---|---|---|
| 0.50 | 0.8799 (`rev5`) | **0.8680** | 100.0% | **lockbox is worse** |
| 0.60 | 0.8933 (`rev5`) | **0.8834** | 95.2% | **lockbox is worse** |
| 0.70 | 0.9099 (`rev5`) | **0.9018** | 89.9% | **lockbox is worse** |
| 0.75 | 0.9231 (`rev5`) | **0.9060** | 86.6% | **lockbox is worse** |
| 0.80 | 0.9383 (`rev5`) | **0.9183** | 81.9% | **lockbox is worse** |
| 0.85 ← shipped | 0.9525 (`rev5`) | **0.9269** | 75.6% | **lockbox is worse** |
| 0.90 | 0.9663 (`rev5`) | **0.9303** | 55.1% | **lockbox is worse** |
| 0.95 | 0.9884 (`rev5`) | **0.9064** | 23.2% | **lockbox is worse** |
| 0.98 | 0.9937 (`rev5`) | **0.9373** | 7.2% | **lockbox is worse** |

The lockbox column is **not** monotonic — see the note under the lockbox table below — so it is a floor that a higher threshold does not raise.

#### Ambiguity reasons — each validated by what it suppressed

`accuracy of guess` is how often the suppressed guess *would* have been right. A reason earns its place by sitting well below the confident set; two reason codes have been removed for failing exactly this test.

| reason | rows | accuracy of the suppressed guess |
|---|---|---|
| `(confident, kept)` | 1,053,405 | 0.9901 |
| `near_transition` | 69,600 | 0.6090 |
| `weight_shift_or_step` | 45,757 | 0.8767 |
| `model_split` | 42,576 | 0.8312 |
| `out_of_distribution` | 14,914 | 0.8427 |
| `posture_shift` | 11,698 | 0.9367 |
| `low_excursion_gait` | 6,342 | 0.7108 |

Independent check: the model abstains on **55.8%** of the 26,533 rows a human marked `-1`, against 15.3% elsewhere. `-1` never enters training, so the agreement is not circular.

#### Lockbox — rev8, held out of everything

| population | coverage | accuracy on committed |
|---|---|---|
| development subjects | 84.66% | 0.9901 |
| **rev8 (lockbox)** | **75.6%** | **0.9269** |

**The 95% target is met on development subjects and missed on the lockbox.** Treat every development number above as an upper bound, not an estimate. The lockbox is single-use — check `caveats.md` §3.2 before re-reading it.

**Accuracy is non-monotonic in the threshold here** — 0.9303 at 0.90 falls to 0.9064 at 0.95. Abstention is supposed to buy accuracy monotonically; on this subject it does not, so the threshold chosen on development subjects is not transferable and the `worst subject` column above is an optimistic floor.

### Direction of error — what pooled accuracy hides

Row level, leave-one-rev-out, at the shipped threshold 0.85: 1,053,405 committed, 190,887 abstained. **Rows, not windows** — the unit a caller receives, and the same unit as the curve above.

| truth → guess | rows |
|---|---|
| walk → walk | 945,747 |
| stand → stand | 97,267 |
| stand → walk ⚠ | 9,364 |
| walk → stand ⚠ | 1,027 |

**9,364 of 10,391 committed errors (90%) are stand called walk.** The residual failure is one-directional, so watch **stand recall**, not accuracy — pooled accuracy is the anaesthetic (`caveats.md` §3.0).

### Top features by importance

| feature | importance | rank |
|---|---|---|
| `ang_LR_corr` | 0.1114 | 1 of 38 |
| `ileg_minhalf` | 0.0918 | 2 of 38 |
| `ileg_swaps` | 0.0523 | 3 of 38 |
| `L_ang_LPF_std` | 0.0481 | 4 of 38 |
| `ileg_minquarter` | 0.0470 | 5 of 38 |
| `R_ang_p95_p05` | 0.0452 | 6 of 38 |
| `L_ang_LPF_ptp` | 0.0438 | 7 of 38 |
| `R_ang_LPF_ptp` | 0.0425 | 8 of 38 |
| `L_ang_p95_p05` | 0.0404 | 9 of 38 |
| `R_angvel_LPF_absmean` | 0.0362 | 10 of 38 |
| `R_ang_LPF_std` | 0.0361 | 11 of 38 |
| `L_angvel_LPF_std` | 0.0357 | 12 of 38 |

The `ileg_*` block carries **19.1%** of total importance. That is the same interleg signal S3's swap rule reads — which is why S3 is no longer an *independent* second opinion (`caveats.md` §1.1b), and why physics catches so few of S2's confident errors.

---

## §3 — S3 physics

*Source: `runs/regen/s3_physics/`*

### What a *window* is — the unit every count below uses

| property | value | why |
|---|---|---|
| length | **2.0 s** = 200 samples @ 100 Hz | ~2 gait cycles — the shortest span that can show a leg swap |
| training stride | 2.0 s (**non-overlapping**) | overlap inflates the effective sample count; `LeaveOneGroupOut(rev)` makes fold leakage impossible either way |
| inference stride | **0.25 s** | a row is labelled by whichever windows cover it |
| bounded by | **segment**, never a file | a window may never span a gap — that would invent data |
| label rule | vote over rows, human `-1` dropped | *label-pure* = the surviving vote is unanimous |

### Where the swap rule is used, and how much it is worth in each place

The rule has zero fitted parameters and never sees a label. Both are real; what they buy depends on what it is pointed at.

| consumer | pointed at | worth |
|---|---|---|
| `label_audit.py` | the **annotations** | **strong** — the rule is genuinely independent of the labels, so a disagreement is evidence about them |
| `plausibility.py` | a whole **file**, against the model | **moderate** — weak per window (see below), but a file-level contradiction is a diagnosable fault rather than a hard window |
| `label.py` physics gate | single **windows**, against the model | **weak, ships OFF** — S2 absorbed `ileg_swaps` and `ileg_minhalf`, so both opinions read the same 1 Hz-filtered interleg angle and are wrong together |


The independence claim was retracted in `anchors.py` on 2026-08-03 and the retraction is what sizes that last row: physics contradicts only ~12% of S2's high-confidence errors and none at p >= 0.95. `roweval` measures the gate against simply raising the threshold rather than leaving it an argument.

### Rate-invariance audit — body or clock?

Every window decimated to half rate through S1's anti-aliasing FIR and the anchors recomputed, over the 3,831 walking windows that could be padded on both sides. §2.3 is why: an earlier experiment read clustering that partitioned by acquisition rate, which turned out to be timestamp quantization — the geometry was measuring the clock.

| anchor | metric | median Δ | verdict |
|---|---|---|---|
| `periodicity` | abs | 0.0273 | **invariant** |
| `antiphase` | abs | 0.0005 | **invariant** |
| `grav_stab` | abs | 0.0001 | **invariant** |
| `gyro_energy` | rel | 0.4994 | **rate_dependent** |


`gyro_energy` is the **negative control** and is defined the way that fails, on purpose: it sums over samples, so halving the sample count halves it. It fired, so the passes above mean something (§11.1).

### Label audit — where the annotations contradict the physics

Model-free by construction: it reads the window grid and the swap rule, never a model, a probability or a prediction. Flagging the files the classifier dislikes would delete precisely the hard cases, improve every metric and teach nothing.

**Broken** — `disagree > 0.5` over ≥20 windows. The file contradicts itself more often than it agrees: a swapped channel or a misaligned label track.

| rev | trial | windows | disagree | fraction |
|---|---|---|---|---|
| rev13 | 4 | 62 | 59 | **0.9516** |

**Divergent convention** — share of in-band windows annotated `walk`, against a corpus rate of 0.872. **Do not exclude these.** A self-consistent minority convention is not bad data; it is the part of the residual error that is annotation policy rather than model failure. The model learns the corpus convention, so a divergent trial reads as model error and no retraining removes it.

| rev | trial | band windows | band walk | vs corpus | p |
|---|---|---|---|---|---|
| rev6 | 2 | 22 | **0.318** | -0.554 | 2.8e-09 |
| rev8 | 4 | 52 | **0.692** | -0.180 | 0.00056 |
| rev7 | 5 | 135 | **0.719** | -0.153 | 1.9e-06 |
| rev6 | 1 | 170 | **0.776** | -0.095 | 0.00051 |
| rev6 | 4 | 139 | **0.777** | -0.095 | 0.002 |
| rev2 | 4 | 79 | **0.987** | +0.115 | 0.00053 |
| rev2 | 5 | 133 | **1.000** | +0.128 | 2.6e-08 |
| rev2 | 3 | 83 | **1.000** | +0.128 | 2e-05 |

---

## §5 — Corpus sweep (`label_all`)

*Source: `labeled_raw/`*

**No ground truth exists here.** This section reports coverage — what fraction of rows cleared the threshold. Whether those calls are right is §2's question, measured on annotated data. Quoting a number from here as accuracy is the single easiest mistake to make in this repo.

|  | count |
|---|---|
| files swept | 91 |
| labelled | **78** |
| refused | **13** |
| rows labelled | 2,594,823 |
| rows committed | 2,209,944 (**85.2%**) |
| rows abstained | 384,879 |
| committed class balance | walk 2,014,685 · stand 195,259 (**91.2% walk**) |

**Partition gate: 78 labelled + 13 refused = 91 of 91, 0 unaccounted.**

### Refusals — a stated refusal, not a low score

A refusal is the pipeline declining to serve a file, with a reason. It is not a bad score and it is not a crash: `label_all` records it and keeps going, and every raw file is still accounted for exactly once.

| refusal | files | cause |
|---|---|---|
| `UnknownVariantError` | 13 | no measured sagittal-axis mapping for variant 'e5f2660f' |

### Coverage is not uniform — the corpus mean hides the shape

| percentile | confident fraction |
|---|---|
| min | 0.1487 |
| p10 | 0.3006 |
| **median** | **0.8986** |
| p90 | 0.9974 |
| max | 1.0000 |

**12 of 78 files commit to less than 50% of their rows.** Listed by name because a corpus average cannot be acted on and a filename can:

| confident | rows | session | variant | dominant reason |
|---|---|---|---|---|
| 0.149 | 10,420 | 20260109 | `fb5ea2c2` | `model_split` |
| 0.177 | 33,130 | 20260108 | `fb5ea2c2` | `model_split` |
| 0.181 | 9,630 | 20260109 | `fb5ea2c2` | `model_split` |
| 0.193 | 10,490 | 20260107 | `fb5ea2c2` | `model_split` |
| 0.201 | 68,910 | 20260109 | `fb5ea2c2` | `model_split` |
| 0.227 | 9,332 | 20260109 | `fb5ea2c2` | `model_split` |
| 0.290 | 34,920 | 20251230 | `fb5ea2c2` | `model_split` |
| 0.301 | 30,350 | 20251230 | `fb5ea2c2` | `model_split` |
| 0.360 | 40,690 | 20251230 | `fb5ea2c2` | `model_split` |
| 0.362 | 61,180 | 20260108 | `fb5ea2c2` | `model_split` |
| 0.363 | 8,730 | 20251230 | `fb5ea2c2` | `model_split` |
| 0.393 | 62,120 | 20260109 | `fb5ea2c2` | `model_split` |

### Dominant abstention reason, by file

| reason | files where it dominates |
|---|---|
| `near_transition` | 50 |
| `model_split` | 18 |
| `uncovered` | 6 |
| `out_of_distribution` | 2 |
| `weight_shift_or_step` | 1 |
| `(none)` | 1 |


### Coverage by session — is it the hardware or the dates?

Session is independent of rev and is the one axis nothing else in this repo cuts. Shown within variant, for the variants that hold a low-coverage file: a cause that is the *hardware* makes every session carrying it bad, a cause that is the *dates* does not.

**`fb5ea2c2`** — 62 files over 12 sessions

| session | files | median coverage | files < 50% |
|---|---|---|---|
| `20251230` | 4 | 0.360 | **4** |
| `20260107` | 1 | 0.193 | **1** |
| `20260108` | 3 | 0.362 | **2** |
| `20260109` | 5 | 0.201 | **5** |
| `20260114` | 7 | 0.973 | 0 |
| `20260115` | 3 | 0.717 | 0 |
| `20260119` | 1 | 0.582 | 0 |
| `20260120` | 7 | 0.887 | 0 |
| `20260121` | 12 | 0.996 | 0 |
| `20260123` | 1 | 0.833 | 0 |
| `20260126` | 3 | 0.759 | 0 |
| `20260128` | 15 | 0.994 | 0 |

**4 of 12 sessions hold every low-coverage file on this variant; the other 8 hold none.** The same hardware records fine outside 20251230–20260109, so the variant is not the cause — those dates are. Look for what changed about the rig or the protocol in that window, not at the device revision.


### Operating points — what each costs here, what each buys there

*Coverage from `labeled_raw/preset_sweep.json` (this corpus, no ground truth); accuracy from `runs/regen/s2_ml/roweval_loro.json` (annotated corpus, held out per subject). Two populations — read across the row, never down one column.*

| threshold | coverage HERE | rows committed | files < 50% | accuracy THERE | worst subject THERE |
|---|---|---|---|---|---|
| 0.50 | 99.9% | 2,593,283 | 0 | 0.9555 | 0.8799 (`rev5`) |
| 0.60 | 96.0% | 2,491,850 | 0 | 0.9682 | 0.8933 (`rev5`) |
| 0.70 `high_coverage` | 92.0% | 2,386,292 | 4 | 0.9784 | 0.9099 (`rev5`) |
| 0.75 | 90.0% | 2,336,070 | 8 | 0.9824 | 0.9231 (`rev5`) |
| 0.80 | 87.6% | 2,273,793 | 9 | 0.9866 | 0.9383 (`rev5`) |
| **0.85** `balanced` ← shipped | 85.2% | 2,210,144 | 12 | 0.9901 | 0.9525 (`rev5`) |
| 0.90 | 82.6% | 2,142,872 | 14 | 0.9942 | 0.9663 (`rev5`) |
| 0.95 `high_precision` | 77.0% | 1,999,043 | 19 | 0.9980 | 0.9884 (`rev5`) |
| 0.98 | 69.6% | 1,807,018 | 29 | 0.9994 | 0.9937 (`rev5`) |

Swept over 78 labelled files (2,594,823 rows) in the single scoring pass `label_all` already makes — a row's confidence does not depend on the threshold, so every point above costs nothing beyond the labelling itself. `label_all` verifies that identity against the frame's own `ambiguous` column at the shipped threshold and refuses to write this artifact if they disagree.

**The two halves count different denominators.** `coverage HERE` is over every row in the file, including rows no full window covers; the development curve counts only rows that were covered AND carry a trainable label. The gap is measured by the 0.50 row above, which reads 99.9% rather than 100%: **0.06% of corpus rows are uncovered at every threshold** and are abstentions of the recording, not of the model.

*Resolution: `confidence` is emitted rounded to 4 decimals while the abstention was decided on the unrounded float, so rows printing exactly a threshold cannot be re-decided from the column. That band holds 4,352 rows at its widest (0.98) and 450 at the shipped point — under 0.3% of committed rows everywhere, and always at the threshold where the ensemble is most evenly split.*

**At 0.95, 1 file(s) commit to nothing at all.** A file that labels every row `-1` is not a strict result, it is an unserved recording that did not refuse — worth reading beside the refusal table above, which counts only files that declined outright.

---

## §B — What is missing from this report

An absent artifact is a stated gap, never a skipped section: a stage that did not run must not read as a stage with nothing to say.

*Nothing. Every artifact this report reads was present.*

